export const FRONTEND_URL = process?.env?.FRONTEND_URL ?? "http://localhost:5173/"
export const BACKEND_URL = process?.env?.VITE_API_URL ?? "http://localhost:8080/"
export const AUTH0_USERNAME = process?.env?.AUTH0_USERNAME ?? "juanignaciot640@gmail.com"
export const AUTH0_PASSWORD = process?.env?.AUTH0_PASSWORD ?? "juani1234!"
